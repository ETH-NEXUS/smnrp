version: "3"
volumes:
  smnrp_live:
  analytics_data:
services:
  smnrp:
    build: .
    ports: 
      - "80:80"
      - "443:443"
    volumes:
      - smnrp_live:/etc/letsencrypt/live
      - analytics_data:/var/log/analytics
    env_file: .env
